# IGDB API Credentials (from Twitch Developer Portal)
IGDB_CLIENT_ID=your_client_id_here
IGDB_CLIENT_SECRET=your_client_secret_here

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration
FRONTEND_URL=http://localhost:3000

# Database (optional, defaults to ./data/voting.db)
# DATABASE_PATH=/path/to/voting.db

# Use mock in-memory DB when better-sqlite3 bindings are missing (e.g. Windows without build tools). No persistence.
# USE_MOCK_DB=1

# Admin secret for protected endpoints (ladder start/close, vote clear).
# If empty, admin endpoints are open to everyone.
# ADMIN_SECRET=your_secret_here
