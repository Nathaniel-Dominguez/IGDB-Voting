# Backend API base URL (used at build time; default: http://localhost:3001/api)
# Local:  http://localhost:3001/api
# Production (e.g. Render): https://igdb-voting-backend.onrender.com/api
# For GitHub Pages deploy, set repo variable VITE_APP_API_URL in Settings → Variables.
# VITE_APP_API_URL=http://localhost:3001/api

# Single server per deployment: set to your Discord server (guild) ID
# so the web app uses that server's ladder. For local dev you can omit
# this and pass ?guildId=... in the URL instead.
# VITE_APP_GUILD_ID=your_discord_server_id
